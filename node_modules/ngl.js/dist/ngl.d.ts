interface Proxy {
    host: string;
    port: number;
    auth: {
        username: string;
        password: string;
    };
}
declare class NGL {
    private url;
    private xurl;
    private proxy?;
    constructor(proxy?: Proxy);
    sendMessage(username: string, message: string): Promise<any>;
}
export { NGL };
